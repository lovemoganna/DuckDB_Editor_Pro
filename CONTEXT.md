# DuckDB Manager Pro Ontology Reasoning

本上下文定义 Ontology 原生组合推演的业务语言。Ontology 描述世界；组合推演在隔离快照中探索这个世界可能变成什么。

## Language

**属性定义（Property Definition）**：
属于某一对象类型、具有稳定标识和值类型的属性契约。真实对象 JSON 中发现的键只有在类型一致并被确认后，才能成为激活的属性定义。
_Avoid_：业务特征、物理列、任意 JSON 键。

**事实（Fact）**：
当前 Ontology 明确记录的对象属性值、有向关系或派生结论。事实必须保留其对象、关系或规则版本来源。
_Avoid_：假设、说明文字、默认值。

**未知（UNKNOWN）**：
某项事实不存在、不可解析或没有足够依据时的真值；既不是真，也不是假。
_Avoid_：默认 FALSE、空字符串、零值。

**假设（Assumption）**：
仅在一次推演场景中覆盖或补充事实的只读输入。假设不会写回真实 Ontology。
_Avoid_：编辑对象、执行动作、规则效果。

**规则绑定（Rule Binding）**：
把规则声明的对象类型变量匹配到当前 Ontology 中具体对象实例的映射。
_Avoid_：按对象名称硬编码、固定案例节点。

**规则（Rule）**：
带版本的结构化条件树及效果集合。条件可以检查属性、关系和派生结论；效果可以改变隔离世界中的属性、关系和派生结论。
_Avoid_：概率模型、自然语言描述、一次性场景。

**动作定义（Action Definition）**：
带对象类型变量、前置条件和效果的结构化变化能力。现有描述性动作没有结构化定义时不可执行。
_Avoid_：动作实例、说明文字推断。

**世界状态（World State）**：
某个推演步骤中的完整属性事实、关系事实和派生结论集合。
_Avoid_：当前界面状态、真实 Ontology 写回结果。

**分支（Branch）**：
规则对同一事实产生无法由优先级裁决的互斥效果时形成的独立世界状态。每个分支都保留自己的成立路径。
_Avoid_：错误、随机结果。

**推演路径（Reasoning Path）**：
从初始事实或假设，经对象绑定、动作和规则触发，到最终世界状态的可追溯证明链。
_Avoid_：静态说明文字、预制结果。

**缺失条件（Missing Condition）**：
使某条规则或目标结论尚不能成立的最小未满足条件。
_Avoid_：失败原因汇总、UNKNOWN 的自动否定。

**反事实建议（Counterfactual）**：
在限定编辑距离内，使另一个规则或结果可能成立的最小事实或动作改变集合。
_Avoid_：直接修改真实 Ontology、未经规则支持的建议。

**推演运行（Reasoning Run）**：
不可变的 Ontology 快照、场景、限制参数和推演报告记录，可按保存时的世界重放。
_Avoid_：当前页面状态、实时写回任务。

**统计证据（Statistical Evidence）**：
由明确样本总体支持的频率或条件概率。它可以作为未来的附加证据，但不能替代 Ontology 事实或符号规则。
_Avoid_：规则命中率、关系权重、主观评分。

**特征（Feature）**：
从稳定属性定义或有向关系定义投影出的、可参与组合的原生 Ontology 条件。特征保留对象类型、属性或关系 ID 与快照来源。
_Avoid_：独立于 Ontology 的业务标签、按名称猜测的信号、固定模板字段。

**候选情形（Situation Candidate）**：
由选定特征的可观测值、声明域与规则边界主动排列出的有界组合。候选先生成，再由约束规则判定为已成立、可能出现或已排除。
_Avoid_：只有规则命中后才产生的结果、静态演示卡片。

**可解释排序（Explainable Ranking）**：
依据证据覆盖、可靠性、条件满足度、冲突、未知事实、历史验证与人工权重计算的候选优先顺序；每个分项与原因必须随结果返回。
_Avoid_：无依据的概率、隐藏评分、把出现频率等同于成立概率。

**规则 AST（Rule AST）**：
系统唯一的结构化条件树协议。AND、OR、NOT 及上下文叶节点共享同一序列化模型；中文 Lisp、图形树、SQL、推演与解释均从它派生。
_Avoid_：为不同视图重复保存规则文本或另一套条件树。
