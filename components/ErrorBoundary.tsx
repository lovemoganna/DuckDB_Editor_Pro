/**
 * ErrorBoundary — re-exports the shared UI boundary.
 * Prefer importing from `components/ui/ErrorBoundary` for new code.
 * This shim keeps Learn/MarkdownViewer and other legacy imports working.
 */
export { ErrorBoundary } from './ui/ErrorBoundary';
